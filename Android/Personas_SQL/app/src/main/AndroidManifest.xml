<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="co.edu.uptc.usuariossqlite">

    <application
        android:allowBackup="true"
        android:icon="@drawable/sqlite"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".logic.PantallaPrincipal" />
        <activity android:name=".logic.CrearPersona" />
        <activity android:name=".logic.ListaSQLiteActivity" />
        <activity android:name=".logic.ModificarPersona" />
    </application>

</manifest>